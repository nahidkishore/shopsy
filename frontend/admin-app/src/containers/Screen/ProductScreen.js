import React from 'react';
import Layout from '../../components/Layout/Layout';
const ProductScreen = () => {
  return (
    <Layout sidebar>
      Products
   </Layout>
  );
};

export default ProductScreen;